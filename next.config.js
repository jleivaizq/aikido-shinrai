/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["aikido-shinrai.local", "aikido-shinrai.000webhostapp.com"]
  }
}

module.exports = nextConfig
